#include <bits/stdc++.h>
using namespace std;
vector<pair<double, int>> kq, ds;
int main()
{
    int n, tt=1, i = 0;
    string n;
    while(getline(cin, n)){
        int sl = n.size(), vt = n.find(' ', sl-1);
        int sbd = (int)stod(n.substr(0, vt-1));
        double diem = stod(n.substr(vt+1, sl-1));
    }
    return 0;
}
