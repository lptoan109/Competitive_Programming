#include <bits/stdc++.h>
using namespace std;
#define ll long long
const int MAXN = 1e6;
ll n,k, a[MAXN+5], dp[MAXN+5];
int kvt[MAXN+5], kqvt;
vector <int> kqv;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    freopen("GAME.INP", "r", stdin);
    freopen("GAME.OUT", "w", stdout);
    cin >> n >> k;
    for(int i = 1; i<=n; ++i){
        cin >> a[i];
    }
    for(int i = 1; i<=n; ++i){
        ;
    }
    return 0;
}
