#include <bits/stdc++.h>
using namespace std;
int t;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    freopen("CAU2.INP", "r", stdin);
    freopen("CAU2.OUT", "w", stdout);
    cin >> t;
    while(t--){
        string s;
        cin >> s;
        int gh = s.size();
        for(int i = 0; i<gh; ++i){

        }
    }
    return 0;
}
