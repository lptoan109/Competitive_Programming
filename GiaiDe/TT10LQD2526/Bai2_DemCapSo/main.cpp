#include <bits/stdc++.h>
using namespace std;
const int MAXN = 1e5;
long long n;
map<long long, int> cnt;
unsigned long long kq;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cin >> n;
    for(int i = 1; i<=n; ++i){
        long long t;
        cin >> t;
        cnt[t]++;
    }
    for(auto &i:cnt){
        kq = kq+()
    }
    return 0;
}
