#include <iostream>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    freopen("OATH.INP", "r", stdin);
    freopen("OATH.OUT", "w", stdout);
    cout << "Yes, I've read and understood all the contest rules.";
    return 0;
}
