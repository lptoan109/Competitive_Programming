#include <bits/stdc++.h>
using namespace std;
int ab, xy, cd, zt,kq
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cin >> ab >> xy >> >cd >> zt;

    if(ab<cd){
        if(ab==cd){
            for(ll i = ab; i<=cd; ++i){
                for(ll j = xy; j<=)
            }
        }
    }
    return 0;
}
