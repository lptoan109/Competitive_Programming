#include <bits/stdc++.h>
using namespace std;
int n;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    freopen("HTTP.INP", "r", stdin);
    freopen("HTTP.OUT", "w", stdout);
    cin >> n;
    if(n>=400) cout << "YES";
    else cout << "NO";
    return 0;
}
